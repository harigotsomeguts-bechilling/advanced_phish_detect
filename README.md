
## Usage

To run the advanced phishing threat tracker locally in your Kali terminal, execute the script and provide the target URL as an argument:

```bash
python3 advanced_phish_detect.py "https://example.com"
```
